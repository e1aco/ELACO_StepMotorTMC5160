# /ela reindex — 重建索引

## 功能
重新扫描 Require/，生成 `require-index.md`（≤50行）。

## 触发
```
/ela reindex
```

## 触发时机
- `/ela result` 通过 + 留痕后（自动）
- `/ela rq` 修改核心文档后（自动）
- 手动新增/删除 Require/ 文件（手动）
- PDF 内容变更（手动）

## 相关
- `commands/req.md` — 知识库详情
